global:
  scrape_interval: 15s # 15초마다 메트릭 수집

scrape_configs:
  - job_name: 'spring-boot-app'
    metrics_path: '/actuator/prometheus' # 스프링 부트 프로메테우스 엔드포인트
    scrape_interval: 5s # 테스트를 위해 5초마다 수집
    static_configs:
      - targets: ['host.docker.internal:8080'] # 도커 내부에서 내 컴퓨터(스프링)로 접근하는 주소